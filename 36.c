#include<stdio.h>
int main()
{
    int a, b;
    printf("Enter the valu of a&b");
    scanf("%d%d", &a, &b);
    if (a>b){
        printf("a is max");
    }
    else{
        printf("b is max");
    }
}
